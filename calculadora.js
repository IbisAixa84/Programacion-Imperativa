/* const prompt = require("prompt-sync")({ sigint: true });


let sumar = (numeroA, numeroB) => numeroA + numeroB    
console.log(sumar(5, 5))

let restar = (numeroC, numeroD) => numeroC - numeroD 
console.log(restar(5, 5))

let multiplicar = (numeroE, numeroF) => numeroE * numeroF
console.log(multiplicar(5, 5))

let dividir = (numeroG, numeroH) => numeroG / numeroH
console.log(dividir(20,4))


console.log("-------------- Testeo de Operaciones / Calculadora --------------");


let numero1 = parseInt(prompt('Ingrese 1er numero: '))
let numero2 = parseInt(prompt('Ingrese 2do numero: '))

console.log(`sumar = ${sumar(numero1, numero2)}`)
console.log(`restar = ${restar(numero1, numero2)}`)
console.log(`multiplicar = ${multiplicar(numero1, numero2)}`)
console.log(`dividir = ${dividir(numero1, numero2)}`)


// Crear una función que se llame cuadradoDeUnNumero, la cual recibe un número como parámetro y deberá retornar ese número elevado al cuadrado.
// Importante: cuadradoDeUnNumero() deberá utilizar la función multiplicar() para calcular el cuadrado del parámetro ingresado a cuadradoDeUnNumero()

let cuadradoDeUnNumero = unNumero => unNumero * unNumero
console.log(cuadradoDeUnNumero(20))


// Crear la función promedioDeTresNumeros, la misma deberá calcular el promedio,de 3 números, que serán ingresados por parámetro.
// Importante: promedioDeTresNumeros() deberá utilizar algunas funciones previamente creadas de nuestra calculadora.

let promedioDeTresNumeros = (numero1, numero2, numero3) => [numero1 + numero2 + numero3] / 3
console.log(promedioDeTresNumeros(2,3,6))


// Crear la función calcularPorcentaje. Esta recibirá dos parámetros: el número total y el porcentaje que se le quiere calcular. Deberá retornar el x% del total.
// Ejemplo: calcularPorcentaje(300, 15) debe retornar 45, ya que es el 15% de 300. Importante: calcularPorcentaje() deberá utilizar algunas funciones previamente
//creadas de nuestra calculadora.

let calcularPorcentaje = (numeroTotal, porcentajeAcalcular) => numeroTotal * porcentajeAcalcular / 100
console.log(calcularPorcentaje(300,15))


// Crear una función GeneradorDePorcentaje que recibe dos parámetros y deberá retornar el porcentaje del primero con respecto al segundo.
// Importante: GeneradorDePorcentaje() deberá utilizar algunas funciones  previamente creadas de nuestra calculadora.
// Ejemplo: GeneradorDePorcentaje(2, 200) deberá retornar 1, ya que 2 es el 1% de 200.
 
let GeneradorDePorcentaje = (x, y) => x / y * 100
console.log(GeneradorDePorcentaje(2,200)) */







for (let i = 0; i < array.length; i++) {
    const element = array[i];
    
} */